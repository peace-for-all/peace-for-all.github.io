# Portfolio publishing policy

This is a low-stakes personal website. When the user and agent explicitly agree on a site change, treat that agreement as authorization to publish it: verify the change, commit it to `main`, push to `origin`, and confirm the GitHub Pages deployment and public result.

Keep changes local only when the user asks for a draft, preview, review before publishing, or otherwise says not to publish.
